<div align="center">

# YggNmap

### Сервис сканирования портов в сети Yggdrasil

[![Лицензия: GPL v3](https://img.shields.io/badge/License-GPLv3-blue.svg)](https://www.gnu.org/licenses/gpl-3.0)
[![Версия Go](https://img.shields.io/badge/Go-1.22+-00ADD8?logo=go)](https://go.dev/)
[![Yggdrasil](https://img.shields.io/badge/Yggdrasil-Network-green)](https://yggdrasil-network.github.io/)

**Сканирование портов без установки для пользователей Yggdrasil**

**Языки:** [🇬🇧 English](README.md) | 🇷🇺 Русский

[Быстрый старт](#быстрый-старт) • [Возможности](#возможности) • [Установка](#установка) • [Документация](#документация)

</div>

---

## Обзор

YggNmap — это бесплатный веб-сервис для сканирования портов, разработанный для сети Yggdrasil. Пользователи просто заходят на сайт, и их IPv6-адрес автоматически сканируется на открытые порты — **установка и настройка не требуются**!

> [!NOTE]
> Этот сервис требует только веб-браузер и активное подключение к Yggdrasil. Оператор сервера запускает nmap, а не конечные пользователи.

### Почему YggNmap?

| Функция | YggNmap | Традиционные инструменты |
|---------|---------|--------------------------|
| **Установка** | Не требуется | Установить nmap локально |
| **Настройка** | Автоматическая | Ручная настройка |
| **Поддерживаемые адреса** | 200::/8 + 300::/8 | Только IPv6 |
| **Прогресс в реальном времени** | WebSocket обновления | Только командная строка |
| **Экспорт результатов** | CSV, JSON, PDF | Ручной парсинг |

---

## Быстрый старт

### Для пользователей

1. **Убедитесь, что Yggdrasil запущен** на вашей машине
2. **Посетите сервис** по адресу `http://[server-ipv6]:8080/`
3. **Нажмите "Быстрое сканирование"** или выберите тип сканирования
4. **Просмотрите результаты** в реальном времени

Вот и всё! Установка nmap не требуется.

### Для операторов сервера

```bash
# Установите зависимости
sudo apt install nmap  # Linux
brew install nmap      # macOS

# Соберите и запустите
go build -o yggnmap
./yggnmap
```

Сервис автоматически обнаружит ваши адреса Yggdrasil и отобразит URL для доступа.

---

## Возможности

### Пользовательский опыт

<details>
<summary><b>Основные функции сканирования</b></summary>

- **Автоматическое определение IP** - Сервер автоматически определяет ваш IPv6 адрес Yggdrasil
- **Три режима сканирования:**
  - **Быстрое сканирование** - Топ 1000 распространённых портов (~1-3 минуты)
  - **Полное сканирование** - Все 65,535 портов (10-30 минут)
  - **Пользовательское сканирование** - Конкретные порты или диапазоны (определяется пользователем)
- **Прогресс в реальном времени** - Живые обновления через WebSocket с полосой прогресса
- **Уведомления об обнаружении портов** - Мгновенные уведомления при обнаружении открытых портов

</details>

<details>
<summary><b>Интерфейс и удобство использования</b></summary>

- **Тёмная тема** - Переключение между светлой и тёмной темами
- **Поддержка нескольких языков** - Интерфейс на английском и русском языках
- **Экспорт результатов** - Скачивание результатов сканирования в форматах CSV, JSON или PDF
- **Адаптивный дизайн** - Работает в браузерах на десктопе и мобильных устройствах
- **Нулевая настройка** - Просто зайдите и сканируйте

</details>

<details>
<summary><b>Поддержка сети Yggdrasil</b></summary>

YggNmap полностью поддерживает оба типа адресов Yggdrasil:

| Тип адреса | Префикс | Пример | Назначение |
|------------|---------|--------|------------|
| **Адреса узлов** | 200::/8 | `200:1234:5678:9abc:def0::1` | Прямые узлы Yggdrasil |
| **Адреса подсетей** | 300::/8 | `300:1234:5678:9abc::1` | Устройства за роутерами |

Оба типа работают одинаково - сервис автоматически определяет и сканирует любой из них.

</details>

### Безопасность и конфиденциальность

> [!IMPORTANT]
> YggNmap реализует комплексные меры безопасности для защиты пользователей и операторов серверов.

<details>
<summary><b>Функции безопасности (Нажмите, чтобы развернуть)</b></summary>

**Защита ввода:**
- Строгая валидация IPv6 (предотвращение CWE-78)
- Предотвращение инъекций команд
- Санитизация инъекций в логи
- Предотвращение обхода путей
- Предотвращение CSV-инъекций (экранирование формул)

**Защита запросов:**
- Валидация CSRF-токенов (истечение через 30 минут)
- Многоуровневое ограничение частоты (по IP + глобальное)
- Ограничение размера запросов (макс. 1 МБ)
- Таймауты запросов (защита от Slowloris)
- Глобальный контроль параллелизма (макс. 10 сканирований)

**Безопасность приложения:**
- HTTP заголовки безопасности (CSP, X-Frame-Options и др.)
- Предотвращение утечек памяти (автоочистка каждые 5 минут)
- Обработка корректного завершения
- Санитизация сообщений об ошибках
- Валидация адресов Yggdrasil (только 200::/8, 300::/8)

**Защита конфиденциальности:**
- **IP клиентов никогда не логируются** - Полная защита конфиденциальности
- Результаты сканирования не хранятся постоянно
- Нет отслеживания пользователей или аналитики
- Только состояние в памяти
- Автоматическое истечение CSRF-токенов

**Безопасность WebSocket:**
- Валидация CSRF для подключений
- Ограничение частоты (5 сек между подключениями)
- Ограничение подключений (макс. 2 на IP)
- Автоматическая очистка (таймаут 60 сек)
- Ограничение размера сообщений (512 байт)

</details>

### Ограничения частоты

Для предотвращения злоупотреблений при разрешении легитимных проверок безопасности:

| Тип сканирования | Ограничение частоты | Глобальное ограничение |
|------------------|---------------------|------------------------|
| Быстрое сканирование | 1 раз в 30 сек на IP | 10 одновременных сканирований |
| Полное сканирование | 1 раз в 60 сек на IP | 10 одновременных сканирований |
| Пользовательское сканирование | 1 раз в 45 сек на IP | 10 одновременных сканирований |

Очистка памяти выполняется каждые 5 минут для удаления старых записей.

---

## Установка

### Предварительные требования

**Для операторов серверов:**

- [Yggdrasil](https://yggdrasil-network.github.io/installation.html) - Подключение к сети
- [nmap](https://nmap.org/) - Инструмент сканирования портов
- [Go 1.22+](https://go.dev/) - Для сборки из исходников

**Для конечных пользователей:**

- Только подключение к Yggdrasil (nmap не нужен!)

### Сборка из исходников

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd yggnmap

# Загрузите зависимости
go mod tidy

# Соберите
go build -o yggnmap

# Запустите
./yggnmap
```

### Кросс-компиляция для нескольких платформ

```bash
# Linux/macOS
./build.sh

# Windows
build.bat
```

Бинарные файлы будут в директории `build/`.

### Запуск сервера

```bash
# По умолчанию: прослушивать все IPv6 интерфейсы, порт 8080
./yggnmap

# Пользовательский порт
./yggnmap -port 9090

# Конкретный IPv6 адрес Yggdrasil
./yggnmap -listen 200:1234:5678::1

# Показать справку
./yggnmap -help
```

**Ожидаемый вывод:**

```
YggNmap - Yggdrasil Network Port Scanner Service
=================================================
Configuration:
  Listen Address: ::
  Port: 8080

Starting YggNmap server on :8080

Detected Yggdrasil addresses:
  - 200:1234:5678:9abc:def0:1122:3344:5566 (node address (200::/8))
  - 300:1234:5678:9abc::1 (subnet address (300::/8))

PRIMARY ACCESS URL: http://[200:1234:5678:9abc:def0:1122:3344:5566]:8080/

Ready to accept connections!
```

### Настройка продакшн сервера (Linux)

Для продакшн развёртывания следуйте этим шагам для создания выделенного пользователя и правильной структуры директорий:

<details>
<summary><b>Шаг 1: Установка зависимостей</b></summary>

```bash
# Ubuntu/Debian
sudo apt update
sudo apt install nmap

# RHEL/CentOS/Fedora
sudo yum install nmap

# Проверьте установку
nmap --version
```

</details>

<details>
<summary><b>Шаг 2: Создание выделенного пользователя</b></summary>

Создайте системного пользователя для запуска сервиса (без возможности входа для безопасности):

```bash
# Создайте системного пользователя без домашней директории и shell для входа
sudo useradd --system --no-create-home --shell /bin/false yggnmap

# Проверьте создание пользователя
id yggnmap
# Вывод: uid=999(yggnmap) gid=999(yggnmap) groups=999(yggnmap)
```

</details>

<details>
<summary><b>Шаг 3: Создание директории приложения</b></summary>

```bash
# Создайте директорию приложения
sudo mkdir -p /opt/yggnmap

# Установите владельца - пользователя yggnmap
sudo chown yggnmap:yggnmap /opt/yggnmap

# Установите соответствующие права доступа
sudo chmod 755 /opt/yggnmap

# Проверьте
ls -ld /opt/yggnmap
# Вывод: drwxr-xr-x 2 yggnmap yggnmap 4096 Jan 4 12:00 /opt/yggnmap
```

</details>

<details>
<summary><b>Шаг 4: Установка бинарного файла</b></summary>

```bash
# Скопируйте скомпилированный бинарник в директорию приложения
sudo cp yggnmap /opt/yggnmap/yggnmap

# Установите владельца - пользователя yggnmap
sudo chown yggnmap:yggnmap /opt/yggnmap/yggnmap

# Сделайте его исполняемым
sudo chmod 755 /opt/yggnmap/yggnmap

# Проверьте
ls -l /opt/yggnmap/yggnmap
# Вывод: -rwxr-xr-x 1 yggnmap yggnmap 12345678 Jan 4 12:00 /opt/yggnmap/yggnmap

# Протестируйте бинарник
/opt/yggnmap/yggnmap -help
```

</details>

<details>
<summary><b>Шаг 5: Настройка firewall (Опционально)</b></summary>

Если у вас запущен firewall, разрешите порт сервиса:

```bash
# UFW (Ubuntu/Debian)
sudo ufw allow 8080/tcp
sudo ufw status

# firewalld (RHEL/CentOS/Fedora)
sudo firewall-cmd --permanent --add-port=8080/tcp
sudo firewall-cmd --reload
sudo firewall-cmd --list-ports

# iptables (вручную)
sudo iptables -A INPUT -p tcp --dport 8080 -j ACCEPT
sudo iptables-save | sudo tee /etc/iptables/rules.v4
```

</details>

<details>
<summary><b>Шаг 6: Создание директории для логов (Опционально)</b></summary>

Если вы хотите перенаправлять логи в файл:

```bash
# Создайте директорию для логов
sudo mkdir -p /var/log/yggnmap

# Установите владельца
sudo chown yggnmap:yggnmap /var/log/yggnmap

# Установите права доступа
sudo chmod 755 /var/log/yggnmap
```

</details>

Теперь переходите к настройке systemd сервиса ниже.

<details>
<summary><b>Настройка Systemd сервиса</b></summary>

Создайте `/etc/systemd/system/yggnmap.service`:

```ini
[Unit]
Description=YggNmap Port Scanner Service
After=network.target yggdrasil.service
Requires=yggdrasil.service

[Service]
Type=simple
User=yggnmap
Group=yggnmap
WorkingDirectory=/opt/yggnmap

# Основная команда запуска сервиса
ExecStart=/opt/yggnmap/yggnmap -listen :: -port 8080

# Настройки перезапуска
Restart=on-failure
RestartSec=5s

# Настройки безопасности
NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=strict
ProtectHome=true
ReadWritePaths=/var/log/yggnmap

# Логирование (опционально - раскомментируйте для включения логирования в файл)
# StandardOutput=append:/var/log/yggnmap/yggnmap.log
# StandardError=append:/var/log/yggnmap/yggnmap.log

[Install]
WantedBy=multi-user.target
```

**Включите и запустите сервис:**

```bash
# Перезагрузите конфигурацию systemd
sudo systemctl daemon-reload

# Включите автозапуск сервиса при загрузке системы
sudo systemctl enable yggnmap

# Запустите сервис
sudo systemctl start yggnmap

# Проверьте статус сервиса
sudo systemctl status yggnmap

# Просмотр логов
sudo journalctl -u yggnmap -f
# Или, если включено логирование в файл:
# sudo tail -f /var/log/yggnmap/yggnmap.log
```

**Полезные команды systemd:**

```bash
# Остановить сервис
sudo systemctl stop yggnmap

# Перезапустить сервис
sudo systemctl restart yggnmap

# Просмотр полных логов
sudo journalctl -u yggnmap --no-pager

# Просмотр последних логов
sudo journalctl -u yggnmap -n 50
```

</details>

### Настройка Reverse Proxy (Опционально)

YggNmap может быть развёрнут за reverse proxy для дополнительных функций, таких как кэширование или балансировка нагрузки.

> [!NOTE]
> Сервер автоматически определяет и использует IP клиента из заголовков прокси (X-Forwarded-For, X-Real-IP).

<details>
<summary><b>Пример конфигурации Nginx</b></summary>

Создайте `/etc/nginx/sites-available/yggnmap`:

```nginx
server {
    listen [::]:80;
    listen 80;
    server_name yggnmap.ygg;

    # Передача реального IP клиента в backend
    location / {
        proxy_pass http://[::1]:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Поддержка WebSocket
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        # Увеличение таймаутов для длительных сканирований
        proxy_read_timeout 900s;
        proxy_send_timeout 900s;
    }
}
```

**Включите и перезапустите Nginx:**

```bash
# Создайте символическую ссылку
sudo ln -s /etc/nginx/sites-available/yggnmap /etc/nginx/sites-enabled/

# Проверьте конфигурацию
sudo nginx -t

# Перезагрузите Nginx
sudo systemctl reload nginx
```

**Важно:** При использовании reverse proxy сервер автоматически определит реальный IP клиента из заголовков `X-Forwarded-For` или `X-Real-IP` и использует его для сканирования и ограничения частоты запросов.

</details>

<details>
<summary><b>Пример конфигурации Caddy (Рекомендуется для Yggdrasil)</b></summary>

Caddy проще настроить и хорошо работает с IPv6:

Создайте `Caddyfile`:

```caddy
http://[200:1234::1]:80 {
    reverse_proxy localhost:8080 {
        # Сохранение реального IP клиента
        header_up X-Real-IP {remote_host}
        header_up X-Forwarded-For {remote_host}

        # Поддержка WebSocket (автоматическая в Caddy)
    }
}
```

**Запустите Caddy:**

```bash
# Установите Caddy
sudo apt install caddy  # Ubuntu/Debian

# Запустите с Caddyfile
sudo caddy run --config Caddyfile

# Или установите как systemd сервис
sudo caddy start --config Caddyfile
```

</details>

---

## Документация

### Архитектура

```
┌─────────────┐                 ┌─────────────┐
│   Браузер   │ ──── HTTP ────> │   Сервер    │
│  (Клиент)   │ <── WebSocket ─ │  (Golang)   │
└─────────────┘                 └──────┬──────┘
                                       │
                                       ▼
                                  ┌─────────┐
                                  │  nmap   │
                                  └─────────┘
```

**Поток данных:**

1. Пользователь заходит на сайт → Сервер определяет IPv6 из запроса
2. JavaScript получает CSRF-токен
3. Пользователь запускает сканирование → Устанавливается WebSocket соединение
4. Сервер валидирует запрос (CSRF, ограничения частоты, параллелизм)
5. nmap сканирует IPv6 клиента с callback'ами прогресса
6. Обновления в реальном времени отправляются через WebSocket
7. Результаты отображаются в браузере
8. Опционально: Экспорт в CSV/JSON/PDF

<details>
<summary><b>Структура проекта</b></summary>

```
yggnmap/
├── main.go                 # Точка входа, CLI, корректное завершение
├── scanner/
│   └── scanner.go          # Обёртка nmap с callback'ами прогресса
├── server/
│   ├── server.go           # HTTP сервер, API endpoints, безопасность
│   └── template.html       # Встроенный веб-интерфейс
├── websocket/
│   └── websocket.go        # WebSocket hub для обновлений в реальном времени
├── export/
│   └── export.go           # Экспорт в CSV, JSON, PDF
├── i18n/
│   └── i18n.go             # Интернационализация (EN/RU)
├── validator/
│   └── validator.go        # Валидация и санитизация ввода
├── yggdrasil/
│   └── detector.go         # Определение адресов Yggdrasil
├── build/                  # Скомпилированные бинарники
├── build.sh/bat            # Скрипты кросс-компиляции
├── go.mod                  # Зависимости
├── README.md               # Документация (английская)
└── README.ru.md            # Этот файл (русская версия)
```

</details>

### API endpoints

| Endpoint | Метод | Описание |
|----------|-------|----------|
| `/` | GET | Веб-интерфейс |
| `/api/info` | GET | Получить IPv6 клиента |
| `/api/csrf-token` | GET | Получить CSRF-токен |
| `/api/quick-scan` | POST | Быстрое сканирование (топ 1000 портов) |
| `/api/scan` | POST | Полное сканирование (все 65535 портов) |
| `/api/custom-scan` | POST | Пользовательское сканирование портов |
| `/api/export/{format}` | GET | Экспорт результатов (csv/json/pdf) |
| `/ws` | WebSocket | Прогресс сканирования в реальном времени |

### Зависимости

```go
require (
    github.com/Ullaakut/nmap/v3       // Go биндинги для nmap
    github.com/gorilla/websocket      // Поддержка WebSocket
    github.com/jung-kurt/gofpdf       // Генерация PDF
)
```

---

## Разработка

### Сборка

```bash
# Стандартная сборка
go build -o yggnmap

# Оптимизированная сборка (меньший бинарник)
go build -ldflags "-s -w" -o yggnmap
```

### Тестирование безопасности

```bash
# Запустить сервер
./yggnmap

# Проверить CSRF защиту
# Откройте консоль браузера - должно появиться "CSRF token acquired"

# Проверить ограничение частоты
# Выполните быстрые сканирования - должны появиться ошибки ограничения

# Проверить параллелизм
# Откройте 10+ вкладок браузера и сканируйте одновременно
```

**Мониторинг логов безопасности:**

```bash
tail -f yggnmap.log | grep SECURITY

# Пример вывода (обратите внимание: IP клиентов не логируются):
# [SECURITY] type=scan_started outcome=success details=map[scan_type:quick]
# [RATE_LIMIT] Quick scan rate limit exceeded
# [WEBSOCKET] Client connected
```

---

## Устранение неполадок

<details>
<summary><b>Частые проблемы сервера</b></summary>

**"nmap not found"**
```bash
# Установите nmap
sudo apt install nmap      # Ubuntu/Debian
sudo yum install nmap      # RHEL/CentOS
brew install nmap          # macOS

# Проверьте установку
which nmap
nmap --version
```

**"Permission denied on port 80/443"**
- Используйте порты > 1024 (например, 8080)
- Или запустите с соответствующими правами
- Порт по умолчанию 8080 работает без root

**"Server not accessible"**
```bash
# Проверьте, что Yggdrasil запущен
yggdrasilctl getSelf

# Проверьте firewall
sudo ufw allow 8080/tcp

# Проверьте, что сервер слушает
netstat -tlnp | grep 8080
```

</details>

<details>
<summary><b>Частые проблемы клиентов</b></summary>

**"Can't connect to service"**
- Убедитесь, что Yggdrasil запущен: `yggdrasilctl getSelf`
- Проверьте правильность URL сервера (используйте скобки: `http://[ipv6]:8080`)
- Попробуйте сначала доступ с сервера локально: `http://localhost:8080`

**"Rate limit exceeded"**
- Это нормально - предотвращает злоупотребления
- Подождите 30-60 секунд между сканированиями
- Быстрое сканирование: задержка 30 сек
- Полное сканирование: задержка 60 сек

**"No open ports found"**
- Это хорошо! Ваш узел защищён
- Убедитесь, что сервисы действительно запущены
- Проверьте правила firewall

</details>

---

## Дорожная карта

### Недавно реализовано

- [x] Прогресс сканирования в реальном времени через WebSocket
- [x] Экспорт результатов (CSV, JSON, PDF)
- [x] Тёмная тема
- [x] Поддержка нескольких языков (EN/RU)
- [x] Пользовательское сканирование портов
- [x] Логирование с защитой конфиденциальности
- [x] Комплексные функции безопасности

### Будущие улучшения

- [ ] Дополнительные языки (ES, ZH, DE)
- [ ] Docker контейнер
- [ ] Больше форматов экспорта (HTML, XML)

---

## Участие в разработке

Мы приветствуем вклад в проект! Не стесняйтесь отправлять issues или pull requests.

### Настройка окружения для разработки

1. Сделайте fork репозитория
2. Создайте ветку для функции: `git checkout -b feature-name`
3. Внесите изменения
4. Запустите тесты: `go test ./...`
5. Закоммитьте: `git commit -am 'Add feature'`
6. Отправьте: `git push origin feature-name`
7. Создайте pull request

### Стиль кода

- Следуйте стандартным соглашениям Go (`gofmt`, `go vet`)
- Добавляйте тесты для новых функций
- Обновляйте документацию
- Держите функции небольшими и сфокусированными
- Обрабатывайте ошибки явно

---

## Лицензия

Этот проект распространяется под лицензией **GNU General Public License v3.0** - см. файл [LICENSE](LICENSE) для деталей.

---

## Отказ от ответственности

> [!WARNING]
> Используйте этот инструмент ответственно. Сканируйте только системы, которыми вы владеете или на сканирование которых у вас есть явное разрешение. Авторы не несут ответственности за неправомерное использование.

---

## Благодарности

- **Yggdrasil Network** - [yggdrasil-network.github.io](https://yggdrasil-network.github.io/)
- **nmap** - [nmap.org](https://nmap.org/)
- **Ullaakut/nmap** - [Go библиотека nmap](https://github.com/Ullaakut/nmap)
- **Gorilla WebSocket** - [gorilla/websocket](https://github.com/gorilla/websocket)
- **gofpdf** - [jung-kurt/gofpdf](https://github.com/jung-kurt/gofpdf)

---

## Поддержка

По вопросам или проблемам:

- Ознакомьтесь с [документацией Yggdrasil](https://yggdrasil-network.github.io/)
- Проверьте [существующие issues](../../issues)
- Создайте [новый issue](../../issues/new)

---

<div align="center">

**Сделано с ❤️ для сообщества сети Yggdrasil**

[⬆ Вернуться наверх](#yggnmap)

</div>
